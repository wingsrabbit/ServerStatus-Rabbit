# v0.130 更新说明

版本状态：历史主线收口版本

来源：

- 基线同步自 GitHub `main`
- 在该基线上完成本地收口整理后推回 `main`

本版本定位：

- 统一版本口径后的最终主线版本
- 在后续 v0.131 NG 升级前，它是 `main` 上的最终收口版本

本次收口内容：

- 以 GitHub `main` 作为本地同步基线
- 保留并纳入 `project-files/` 交接资料目录
- 删除根目录下的 `工程文档-v0.13.md`
- 将 README 与后台页面中的版本文案统一到 `v0.130`
- 保留 `project-files/update/` 中的历史 update 文档，作为版本演进资料
- 计划删除已完成使命的旧 feature 分支，并将仓库统一收敛到 `main`

当前应以什么为准：

- 唯一保留主分支：`main`
- 唯一最终版本号：`v0.130`

备注：

- 这个文件不是历史 changelog，而是最终收口版本说明。
- 在 `ServerStatus-Rabbit-NG` 分支中，当前正式版本已升级到 v0.131。
- 历史 update 文件保留，不影响 v0.130 作为 `main` 的正式版本。